var matrix = [];
var m=10;
var n=10;
for (var y = 0; y < n; y++) {
    matrix[y] = [];
    for (var x = 0; x < m; x++) {
        matrix[y][x] = Math.floor((Math.random() * 5) + 0);;

        }
    }
    module.exports = matrix;
